const saveMedia = require('./saveMedia');
const updateMedia = require('./updateMedia');
const validate_input = require('./validate_input');
const attachMedia = require('./attachMedia');
const detachMedia = require('./detachMedia');
const deleteMedia = require('./deleteMedia');
const prepareMediaGallery = require('./prepareMediaGallery');
const prepareSlider = require('./prepareSlider');

module.exports = {
    saveMedia,
    updateMedia,
    validate_input,
    attachMedia,
    detachMedia,
    deleteMedia,
    prepareMediaGallery,
    prepareSlider,
}