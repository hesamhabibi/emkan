import AdminLayout from "@admin/Layout"
import JoditEditor from "@admin/Input/JoditEditor"
import { useForm } from "react-hook-form"
import Input from "@admin/Input"

export default function Page() {
  const { control } = useForm()

  return (
    <AdminLayout>
      <div />
    </AdminLayout>
  )
}
